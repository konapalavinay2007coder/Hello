{
  "name": "hello-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && echo 'Backend ready'",
    "start": "cd backend && npm start"
  }
}
